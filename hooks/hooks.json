{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|clear",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Claude-Model-Selection plugin: when the first real user message of this chat arrives (not a bare greeting), invoke the claude-model-selection skill once, before starting the task. If it recommends a different model than the current one, stop after the recommendation and wait for the user to switch; start the task only on the next message. Skip it if the user asks to skip it (for example: no selection, skip selection, sin seleccion). Do not repeat it on later messages; mid-chat, only follow the Mid-chat rule of that skill when the task clearly changes in complexity.'"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash -c \"$(tr -d '\\r' < \"${CLAUDE_PLUGIN_ROOT}/hooks/remind-until-run.sh\")\""
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Agent|Task",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c \"$(tr -d '\\r' < \"${CLAUDE_PLUGIN_ROOT}/hooks/check-subagent-model.sh\")\""
          }
        ]
      }
    ]
  }
}
